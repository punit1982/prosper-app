# Prosper — pinned dependencies (April 2026)
# Pin exact versions for reproducibility on Render.
# Update intentionally — run: pip install -r requirements.txt && pytest

streamlit==1.41.1
anthropic>=0.40.0,<1.0.0
python-dotenv==1.0.1
pandas==2.2.3
Pillow>=10.0.0,<12.0.0
requests>=2.31.0,<3.0.0
yfinance==0.2.54
plotly==5.24.1
finnhub-python==2.4.20
openpyxl>=3.1.0,<4.0.0
streamlit-authenticator==0.3.3
scipy>=1.11.0,<2.0.0
numpy>=1.24.0,<3.0.0
PyYAML>=6.0.0,<7.0.0
bcrypt>=4.0.0,<5.0.0
streamlit-google-auth>=1.0.0,<2.0.0

# ── Reliability additions (Phase 1 audit) ────────────────────────────────
# Retry logic for unstable external APIs (yfinance, Finnhub, Twelve Data)
tenacity>=8.2.0,<9.0.0
# Date parsing for Serper news timestamps and RSS feeds
python-dateutil>=2.8.0,<3.0.0
# File locking for concurrent YAML writes (B1 fix — Phase 2)
filelock>=3.12.0,<4.0.0
